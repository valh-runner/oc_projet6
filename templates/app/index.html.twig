{% extends 'base.html.twig' %}

{% block title %}Hello AppController!{% endblock %}

{% block body %}
	{% for trick in tricks %}
		<article>
			<a href="" class="btn btn-primary">{{ trick.name }}</a>
			<div class="content">
				<img src="{{ trick.pictures.first().filename }}" alt="">
			</div>
		</article>
	{% endfor %}
{% endblock %}
